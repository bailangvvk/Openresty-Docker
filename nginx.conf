# 文件路径：./nginx.conf

worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;
        location / {
            default_type text/plain;
            content_by_lua_block {
                ngx.say("Hello from minimal OpenResty!")
            }
        }
    }
}
